# Local country flag assets

These SVG files come from the public-domain `hampusborgos/country-flags`
collection: https://github.com/hampusborgos/country-flags

Only countries referenced by the language registry are included. The customer
menu loads them lazily on desktop; mobile platforms use native Unicode flag
emoji.
