"use client";

import { DashboardView } from "@/components/admin/dashboard-view";

/** The admin root is the restaurant's Home Screen, not another module window. */
export function DashboardViewModule() {
  return <DashboardView />;
}
